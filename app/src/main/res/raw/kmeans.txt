【K-Means 聚类分析】

简介：
K-Means 是一种无监督学习算法，用于将数据划分为 K 个簇（Cluster），使得簇内差异最小，簇间差异最大。

主要步骤：
1. 随机选择 K 个初始中心。
2. 将每个样本分配到最近的中心。
3. 更新簇中心为该簇样本的均值。
4. 重复步骤2-3直到收敛。

使用说明：
1. 上传包含样本特征数据的Excel文件。
2. 算法默认设定聚类数 K=3（目前为固定值）。
3. 程序将绘制聚类散点图（若维度>2，则使用PCA降维展示）。
